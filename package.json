{
  "name": "kotoba-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check runtime-shim.js && node --check llm-provider-shim.js && node scripts/verify-lab.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
